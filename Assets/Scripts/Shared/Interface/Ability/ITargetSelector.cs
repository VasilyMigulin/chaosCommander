namespace Game.Core.Shared.Interface
{
    /// <summary>
    /// Маркер «селектор цели» — фильтр-КАТЕГОРИЯ (враж. существа / свои существа /
    /// враж. игрок / свой игрок). Селекторы объединяются по ИЛИ: кандидат проходит,
    /// если подходит ХОТЯ БЫ ОДНОМУ селектору. Если селекторов в наборе нет — пропускаются
    /// все кандидаты. Обычные ITargetFilter (без этого маркера) — ОГРАНИЧИТЕЛИ: сужают по И.
    /// Так «Enemy + Opponent» = все враги (объединение), а «Enemy + Color» = красные враж. существа.
    /// </summary>
    public interface ITargetSelector : ITargetFilter { }
}
