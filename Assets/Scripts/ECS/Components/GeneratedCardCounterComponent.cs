namespace Game.Core.Ecs.Components
{
    /// <summary>
    /// Счётчик карт, ПОРОЖДЁННЫХ способностями этой карты-источника. Нужен для детерминированного
    /// NetworkEntityKey генерируемых карт: оба клиента прогоняют один и тот же эффект в одном порядке
    /// (пассив ре-ранит резолв из снапшота), поэтому seq совпадает → ключ совпадает → ссылки на карту
    /// резолвятся у обоих. Заменяет недетерминированный Guid.NewGuid() для генерации из эффектов.
    /// </summary>
    public struct GeneratedCardCounterComponent
    {
        public int Next;
    }
}
