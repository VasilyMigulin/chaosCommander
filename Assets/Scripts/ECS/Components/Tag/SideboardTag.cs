namespace Game.Core.Ecs.Components
{
    /// <summary>Карта лежит в «отложенных» (сайдборд, зона Сказочника): НЕ в колоде, НЕ в руке, НЕ на поле.
    /// Заполняется на ините матча из SavedDeckData.Sideboard; забирается способностью носителя (раскопка
    /// с TargetZone.Sideboard), после чего карта уходит в руку обычным путём.
    ///
    /// Зона намеренно НЕ входит в TargetZone.Any: сайдборд — приватный ресурс своей карты, а не общая
    /// свалка, из которой «возьми что-нибудь откуда угодно» могло бы тянуть карты.</summary>
    public struct SideboardTag
    {
    }
}
