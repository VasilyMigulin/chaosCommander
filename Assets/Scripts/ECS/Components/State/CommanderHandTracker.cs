namespace Game.Core.Ecs.Components
{
    /// <summary>
    /// Служебный трекер для RunCommanderCooldownSystem: помнит, был ли командир в руке в прошлом
    /// наблюдении. Нужен, чтобы поймать переход «поле/лимбо → рука» (любой возврат: смерть, баунс,
    /// «вернуть в руку») и повесить кулдаун именно на возврат, а не на конкретную причину.
    /// Наличие компонента = трекер инициализирован (первое наблюдение не срабатывает — стартовая рука
    /// не даёт кулдаун).
    /// </summary>
    public struct CommanderHandTracker
    {
        public bool WasInHand;
    }
}
