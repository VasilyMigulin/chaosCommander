namespace Game.Core.Ecs.Components
{
    /// <summary>
    /// Атрибуция убийства: PlayerId игрока, чьё действие убило это существо. Ставится ВМЕСТЕ с DeadTag
    /// (TakeDamageSystem — летальный урон; Destroy-эффекты — прямое уничтожение), потребляет DieSystem:
    /// убийца-враг владельца → базовый доход маны (единая точка начисления для ВСЕХ способов убийства —
    /// раньше мана капала только за смерти от урона, Голодный повар/потасовка уничтожали «бесплатно»).
    /// СИНК: все пишущие пути исполняются на обоих клиентах (урон/эффекты реплеятся) → зеркально.
    /// </summary>
    public struct KilledByComponent
    {
        public int PlayerId;
    }
}
