namespace Game.Core.Ecs.Components
{
    // === struct (Component) ===
    /// <summary>
    /// Свойство «Вампиризм»: любой урон, который наносит носитель (бой ИЛИ способность), лечит ЕГО
    /// ВЛАДЕЛЬЦА (игрока-аватара, не самого носителя) на ту же величину. Проверяется в
    /// TakeDamageSystem.ApplyDamage — единая точка урона, источник урона (атака/способность) не важен.
    /// Лечит РЕАЛЬНО прошедший урон (после Щита/Неуязвимости — если урон целиком заблокирован,
    /// вампиризм не срабатывает, как и «Ядовитый»/Poisoned).
    /// </summary>
    public struct VampirismTag { }
}
