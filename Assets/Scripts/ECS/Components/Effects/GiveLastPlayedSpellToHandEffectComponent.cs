namespace Game.Core.Ecs.Components
{
    /// <summary>
    /// Даёт владельцу источника копию его последнего разыгранного заклинания
    /// (из LastPlayedSpellComponent). Используется «Попадос».
    /// </summary>
    public struct GiveLastPlayedSpellToHandEffectComponent { }
}
