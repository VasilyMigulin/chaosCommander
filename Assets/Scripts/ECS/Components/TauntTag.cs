namespace Game.Core.Ecs.Components
{
    // === struct (Component) ===
    /// <summary>
    /// Свойство «Защитник»: если атакующий УЖЕ стоит рядом с носителем (без движения), он обязан бить
    /// ТОЛЬКО его — другие цели (существа/аватар) недоступны в этот клик/решение. НЕ ограничивает атаку,
    /// если до Защитника нужно ещё дойти скоростью — тогда цель выбирается свободно, как обычно. Проверяется
    /// в точке ПРИНЯТИЯ РЕШЕНИЯ (RunSelectCellSystem — клик игрока; RunAiTurnSystem — выбор цели ИИ), ДО
    /// начала пути: правило смотрит на позицию атакующего ДО движения, а не после. Маркер, без состояния.
    /// </summary>
    public struct TauntTag { }
}
