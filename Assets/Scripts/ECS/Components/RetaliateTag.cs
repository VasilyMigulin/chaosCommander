namespace Game.Core.Ecs.Components
{
    // === struct (Component) ===
    /// <summary>
    /// Свойство «Ответочка»: если носитель был АТАКОВАН (бой, не способности) и пережил удар — наносит
    /// атаковавшему свой Attack в ответ. Проверяется в TakeDamageSystem сразу после основного удара (общая
    /// ApplyDamage — уважает Щит/Неуязвимость атакующего), БЕЗ повторной проверки Ответочки у атакующего
    /// (иначе два таких существа дали бы бесконечный обмен). Маркер, без состояния.
    /// </summary>
    public struct RetaliateTag { }
}
