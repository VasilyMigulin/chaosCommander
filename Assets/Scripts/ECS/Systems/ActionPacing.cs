namespace Game.Core.Ecs.Systems
{
    // === helper ===
    /// <summary>
    /// Базовая пауза между обработкой СОСЕДНИХ элементов очереди: резолв способностей у актива
    /// (RunResolveAbilityQueueSystem) и реплей действий у пассива (ReplayActionSystem). Зачем:
    ///   • читаемость — каскад эффектов/розыгрышей перестаёт быть «мешанинойй» за 1-2 кадра;
    ///   • сеть — снапшоты (ActionAbilityData/ActionCastData) не улетают пачкой в соседние кадры.
    ///
    /// Это ЧИСТО тайминг: не меняет ни порядок действий, ни игровое состояние (как гейты анимации/снаряда) —
    /// значит на синк/детерминизм не влияет. Тюнится ТУТ в одном месте (базово 0.1с). 0 = без паузы (как раньше).
    /// </summary>
    public static class ActionPacing
    {
        public static float GapSeconds = 0.1f;
    }
}
